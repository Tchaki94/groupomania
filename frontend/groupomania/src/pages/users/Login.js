import React from 'react';
import Navigation from '../../components/Navigation';

const Login = () => {
    return (
        <div>
            <Navigation />
        </div>
    );
};

export default Login;